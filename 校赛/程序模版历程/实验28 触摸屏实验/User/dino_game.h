/**
 ****************************************************************************************************
 * @file        dino_game.h
 * @brief       Chrome 风格恐龙跳跃游戏 - 正点原子探索者 STM32F407 V3
 *
 * 按键说明:
 *   KEY_UP (WK_UP / PA0) : 跳跃 / 开始游戏 / 重新开始
 *   KEY0   (PE4)          : 俯冲（长按）
 *   KEY1   (PE3)          : 死亡后重新开始
 ****************************************************************************************************
 */

#ifndef __DINO_GAME_H
#define __DINO_GAME_H

#include "./SYSTEM/sys/sys.h"

/**
 * @brief  游戏主入口函数 - 在 main() 完成所有 BSP 初始化后调用
 *         函数永不返回（内含无限循环）
 */
void dino_game_run(void);

#endif /* __DINO_GAME_H 恐龙游戏头文件 */
